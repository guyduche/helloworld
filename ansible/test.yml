---
- name: "Availability test"
  become: true
  hosts: clients
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: "test"
      uri:
        url: "http://{{ ansible_host }}"
        return_content: true
      register: this
      failed_when: "'Hello universe! Aurelien' not in this.content"
